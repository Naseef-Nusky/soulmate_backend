# Backend - Soulmate App

Setup:

1) Create `.env` (see `ENV.md`)
2) Install deps: `npm install`
3) Run dev: `npm run dev`

Routes:
- POST `/api/generate` → { answers, birthDetails, email? }

Requires `GEMINI_API_KEY`. Optional: `DATABASE_URL`, `SENDGRID_API_KEY`. 


